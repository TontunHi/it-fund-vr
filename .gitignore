# ---------------------------------
# 1. Dependencies (ไม่ต้องเก็บไฟล์ไลบรารี)
# ---------------------------------
node_modules/
*/node_modules/

# ---------------------------------
# 2. Environment Variables (ความลับ ห้ามหลุด!)
# ---------------------------------
.env
.env.local
.env.*.local
server/.env
client/.env

# ---------------------------------
# 3. Build Outputs (ไฟล์ที่ Build แล้วไม่ต้องเก็บ)
# ---------------------------------
dist/
build/
client/dist/
client/build/

# ---------------------------------
# 4. User Uploads (สำคัญมากสำหรับโปรเจกต์นี้)
# ห้ามเก็บรูปสลีปและใบเสร็จขึ้น Git เพราะจะทำให้ Repository ใหญ่เกินจำเป็น
# ---------------------------------
server/uploads/*
!server/uploads/.gitkeep 
# (บรรทัดบนหมายถึง: ไม่เอาทุกอย่างใน uploads แต่เก็บไฟล์ชื่อ .gitkeep ไว้เพื่อรักษาโครงสร้างโฟลเดอร์)

# ---------------------------------
# 5. Logs & System Files (ไฟล์ขยะ)
# ---------------------------------
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs/
*.log
.DS_Store
Thumbs.db
.vscode/
.idea/